# Optional absolute backend URL (example: http://localhost:8000).
# Keep empty to use Vite proxy (/api).
VITE_API_BASE_URL=

# Vite dev proxy target for /api and websocket traffic.
VITE_DEV_PROXY_TARGET=http://localhost:8000
