# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Optional absolute backend URL (leave empty to use dev proxy).
VITE_API_BASE_URL=

# Vite dev proxy target for /api and websocket traffic.
VITE_DEV_PROXY_TARGET=http://localhost:8000
